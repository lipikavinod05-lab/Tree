int maxDepth(struct TreeNode* root) {
    if(root==NULL)
    {
        return 0;
    }
    if(root->left==NULL && root->right==NULL)
    return 1;

    int lh=lh+maxDepth(root->left);
    int rh=rh+maxDepth(root->right);

    if(lh>rh)
    return lh+1;

    else return rh+1;
}
